/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.dummyjson.com', 'assets.dummyjson.com', 'i.pinimg.com', "cdni.iconscout.com"]
    }
};

export default nextConfig;
