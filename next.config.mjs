/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.dummyjson.com', 'assets.dummyjson.com', 'i.pinimg.com', "cdni.iconscout.com", "www.att.com"]
    }
};

export default nextConfig;
